# Story-To-Image-Generazione-Controllata-Di-Immagini-Personalizzate
Insieme di Storie e risultati derivanti dalla generazione di immagini sequenziali tramite Text-to-Image e Image-to-Image (T2I/I2I). Utilizzate tre pipeline diverse in tre modalità diverse. Una per la generazione di immagini uno specifico target (generico, bambini e ragazzi) , in maniera base (VLM) , Assistita (VLM+LLM) e Estesa (VLM+LLM+LLM). 


🇮🇹 README: Pipeline Ibrida LLM-VLM per Visual Storytelling Coerente📚 Panoramica del ProgettoQuesto repository contiene il codice sorgente (notebooks Jupyter) e i risultati sperimentali relativi alla tesi [INSERISCI QUI IL TITOLO COMPLETO DELLA TUA TESI].Il progetto propone e convalida un pipeline ibrido innovativo che integra un Large Language Model (LLM), per l'ottimizzazione ricorsiva dei prompt, con un Vision-Language Model (VLM) per la generazione sequenziale di storie illustrate. L'obiettivo è superare la mancanza di coerenza visiva e semantica tipica dei VLM autonomi nello visual storytelling.L'analisi si concentra sul confronto di tre configurazioni di pipeline (A, B, C) applicate a tre esperimenti tematici distinti (senza filtro, con filtro per bambini, con filtro per adolescenti).🎯 Obiettivi PrincipaliMassimizzare la Coerenza: Garantire l'integrità narrativa ($\overline{\text{CLIPScore}}$) e la continuità visiva ($\overline{\text{LPIPS}}$) tra i cinque frame di ogni storia.Mitigare le Anomalie: Utilizzare l'LLM e il negative_prompt per ridurre gli artefatti strutturali ($\overline{\text{Anomaly Score}}$).Propagazione dello Stile: Mantenere in modo ricorsivo uno stile visivo specifico (es. "filtro per bambini") su tutta la sequenza.🧪 Le Tre Configurazioni SperimentaliFile NotebookConfigurazioneRuolo dell'LLM e InterventoEsperimento-Experiment A.ipynbVersione A (Base)Linea di Base. VLM puro senza LLM. Misura il fallimento intrinseco.Esperimento-Experiment B.ipynbVersione B (Assistita)LLM per ottimizzazione degli attributi (negative_prompt e stile).Esperimento-Experiment C.ipynbVersione C (Estesa)LLM per ristrutturazione e riscrittura completa del prompt per la massima qualità.🛠️ Guida all'Installazione e all'Esecuzione1. PrerequisitiPer eseguire i notebook e replicare gli esperimenti, è necessario:Python 3.xAccesso API LLM: Chiave API per Google Gemini (usato per l'ottimizzazione del prompt).Accesso API VLM: Chiave API per il servizio di generazione immagini (es. Stability AI / Stable Diffusion).2. Configurazione delle API KeyTutti i notebook utilizzano la libreria getpass per nascondere e proteggere le chiavi API.A. Chiave LLM (Google Gemini)Il tuo codice include una robusta logica di resilienza (retry con exponential backoff) per gestire gli errori API 503/429.Ottieni la tua Gemini API Key.All'inizio del notebook (Cella 2 o 3), quando richiesto, inserisci la chiave nel campo di input:PythonLLM_API_KEY = getpass.getpass('Inserisci la tua Gemini API Key')
client = genai.Client(api_key=LLM_API_KEY)
B. Chiave VLM (Generazione Immagini)Ottieni la chiave del servizio VLM utilizzato (es. Stability AI).Inserisci la chiave nel campo di input quando richiesto all'inizio del notebook:PythonSTABILITY_KEY = getpass.getpass('Enter your API Key')
3. EsecuzioneÈ consigliato eseguire i file in un ambiente Jupyter (es. Google Colab).Apri il notebook desiderato (A, B o C).Esegui le celle in sequenza (o utilizza l'opzione "Esegui tutto" / "Run All").I notebook stamperanno a schermo le immagini generate, i prompt ottimizzati dall'LLM e i grafici delle metriche aggregate alla fine di ogni esecuzione.📊 Metriche di Performance (Indicatori Chiave)Il codice calcola e valuta le immagini in base ai seguenti indicatori:MetricaDescrizioneSoglia Chiave$\overline{\text{CLIPScore}}$Coerenza Semantica. Misura la fedeltà del testo all'immagine.Accettabile: $\ge 25$$\overline{\text{LPIPS}}$Coerenza Visiva. Distanza percettiva tra frame consecutivi.Soglia Simile: $\le 0.50$$\overline{\text{Anomaly Score}}$Anomalie Strutturali. Media del $\overline{\text{CLIPScore}}$ con prompt negativi (e.g., "brutto").Sospetta: $\ge 20.1$$\overline{\text{CLIP-IQA}}$Qualità Percettiva. Valutazione estetica basata su prompt positivi.Varia in base allo stile📧 ContattiPer domande o chiarimenti sulla metodologia, si prega di contattare l'autore:Autore: [Il tuo Nome e Cognome]Email: [La tua Email]Università: [La tua Università]🇬🇧 README: Hybrid LLM-VLM Pipeline for Coherent Visual Storytelling📚 Project OverviewThis repository contains the source code (Jupyter notebooks) and experimental results related to the thesis [INSERT YOUR THESIS TITLE HERE].The project proposes and validates an innovative hybrid pipeline that integrates a Large Language Model (LLM) for recursive prompt optimization with a Vision-Language Model (VLM) for sequential illustrated story generation. The goal is to overcome the lack of visual and semantic coherence typical of standalone VLMs in visual storytelling.The analysis focuses on comparing three pipeline configurations (A, B, C) applied to three distinct thematic experiments (unfiltered, child filter, and adolescent filter).🎯 Key ObjectivesMaximize Coherence: Ensure narrative integrity ($\overline{\text{CLIPScore}}$) and visual continuity ($\overline{\text{LPIPS}}$) across the five frames of each story.Mitigate Anomalies: Utilize the LLM and the negative_prompt to reduce structural artifacts ($\overline{\text{Anomaly Score}}$).Style Propagation: Recursively maintain a specific visual style (e.g., "child filter") throughout the entire sequence.🧪 The Three Experimental ConfigurationsNotebook FileConfigurationLLM Role and InterventionEsperimento-Experiment A.ipynbVersion A (Base)Baseline. Pure VLM with no LLM. Measures intrinsic failure.Esperimento-Experiment B.ipynbVersion B (Assisted)LLM for attribute optimization (negative_prompt and style guidance).Esperimento-Experiment C.ipynbVersion C (Extended)LLM for full prompt restructuring and rewriting for maximum quality.🛠️ Installation and Execution Guide1. PrerequisitesTo run the notebooks and replicate the experiments, you need:Python 3.xLLM API Access: API Key for Google Gemini (used for prompt optimization).VLM API Access: API Key for the image generation service (e.g., Stability AI / Stable Diffusion).2. API Key ConfigurationAll notebooks use the getpass library to hide and protect your API keys.A. LLM Key (Google Gemini)Your code includes robust resilience logic (retry with exponential backoff) to handle 503/429 API errors.Obtain your Gemini API Key.At the beginning of the notebook (Cell 2 or 3), when prompted, enter the key into the input field:PythonLLM_API_KEY = getpass.getpass('Inserisci la tua Gemini API Key')
client = genai.Client(api_key=LLM_API_KEY)
B. VLM Key (Image Generation)Obtain the key for the VLM service used (e.g., Stability AI).Enter the key into the input field when prompted at the start of the notebook:PythonSTABILITY_KEY = getpass.getpass('Enter your API Key')
3. ExecutionIt is recommended to run the files in a Jupyter environment (e.g., Google Colab).Open the desired notebook (A, B, or C).Execute the cells sequentially (or use the "Run All" option).The notebooks will output the generated images, the LLM-optimized prompts, and the aggregated metric graphs upon completion of the execution.📊 Performance Metrics (Key Indicators)The code calculates and evaluates images based on the following indicators:MetricDescriptionKey Threshold$\overline{\text{CLIPScore}}$Semantic Coherence. Measures text-to-image fidelity.Acceptable: $\ge 25$$\overline{\text{LPIPS}}$Visual Coherence. Perceptual distance between consecutive frames.Similar Threshold: $\le 0.50$$\overline{\text{Anomaly Score}}$Structural Anomalies. $\overline{\text{CLIPScore}}$ average with negative prompts (e.g., "ugly").Suspicious: $\ge 20.1$$\overline{\text{CLIP-IQA}}$Perceptual Quality. Aesthetic evaluation based on positive prompts.Varies by style📧 ContactFor questions or clarification regarding the methodology, please contact the author:Author: [Your Full Name]Email: [Your Email Address]University: [Your University]
